{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gua.dev/schema/events.schema.json",
  "title": "Gua Event",
  "type": "object",
  "required": ["type", "timestampMs"],
  "additionalProperties": false,
  "properties": {
    "type": {
      "type": "string",
      "enum": ["click", "focus", "set_value", "set_checked", "select", "scroll", "press_key", "key", "text", "gamepad", "game_input"]
    },
    "timestampMs": {
      "type": "integer",
      "minimum": 0
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "requestId": { "type": "integer", "minimum": 0 },
    "status": { "type": "string", "enum": ["succeeded", "failed"] },
    "error": { "type": "string", "enum": ["node_not_found", "hidden", "disabled", "unsupported", "invalid_value"] },
    "sensitive": { "type": "boolean", "default": false },
    "checked": { "type": "boolean" },
    "deltaX": { "type": "number" },
    "deltaY": { "type": "number" },
    "key": {
      "type": "string",
      "minLength": 1
    },
    "text": {
      "type": "string"
    },
    "axis": {
      "type": "string"
    },
    "operation": { "type": "string" },
    "actionId": { "type": "string", "pattern": "^[a-z][a-z0-9_.-]*$" },
    "deviceIndex": { "type": "integer", "minimum": 0 },
    "leaseMs": { "type": "integer", "minimum": 0, "maximum": 60000 },
    "value": {
      "oneOf": [
        { "type": "string" },
        { "type": "number", "minimum": -1, "maximum": 1 }
      ]
    }
  },
  "allOf": [
    {
      "if": { "properties": { "type": { "enum": ["click", "focus", "set_value", "set_checked", "select", "scroll", "press_key"] } } },
      "then": { "required": ["requestId", "status"] }
    },
    {
      "if": { "properties": { "sensitive": { "const": true } }, "required": ["sensitive"] },
      "then": { "properties": { "value": { "type": "string", "maxLength": 0 } } }
    }
  ]
}
